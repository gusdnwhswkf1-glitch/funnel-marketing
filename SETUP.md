# 퍼널마케팅 랜딩페이지 — 세팅 가이드

파일 3개가 전부입니다.

| 파일 | 용도 |
|---|---|
| `index.html` | 사이트 본체. 이거 하나만 올리면 됩니다 |
| `apps-script.gs` | 시트 연동 코드. 구글 시트에 붙여넣는 용도 |
| `SETUP.md` | 이 문서 |

---

## 1. 지금 상태로도 동작합니다

`index.html` 을 더블클릭하면 바로 열립니다.
시트 연동 전에는 상담 신청 버튼이 **메일 앱을 여는 방식**으로 동작합니다.

> ⚠️ 다만 모바일에서 메일 앱을 안 쓰는 사람은 버튼을 눌러도 아무 일이 안 일어납니다.
> 광고를 돌리실 거면 **아래 2번을 먼저 끝내고** 배포하세요.

---

## 2. 문의접수 시트에 직결하기 (10분)

방문자가 신청하면 **「문의접수」 시트 3행부터 빈 줄을 찾아 자동으로 한 줄이 꽂힙니다.**
별도 응답 탭이 생기지 않고, 드롭다운·자동계산·요약 집계가 전부 그대로 살아있습니다.

### 채워지는 열

| 열 | 값 | 비고 |
|---|---|---|
| A 문의번호 | — | **건드리지 않음** (수식 유지) |
| B 접수일 | 신청한 날짜 | 자동 |
| C 유입경로 | `랜딩페이지` | 자동 · 요약 탭에 바로 집계됨 |
| D 회사명 | 방문자 입력 | 필수 |
| E 회사구분 | 방문자 선택 | 드롭다운 |
| F 담당자 | 방문자 입력 | 필수 |
| G 연락처 | 방문자 입력 | 필수 |
| I 현장명 | 방문자 입력 | 선택 |
| J 지역 | 방문자 입력 | 선택 |
| R 문의 내용 | 방문자 입력 | 선택 |
| S 진행 상태 | `신규접수` | 자동 |
| W D-Day | — | **건드리지 않음** (수식 유지) |

나머지 열(이메일·분양유형·세대수·오픈시기·요청서비스·예산대·내부담당·다음액션 등)은 **비워둡니다.**
통화하면서 직접 채우시는 칸입니다.

### 2-1. 스크립트 붙여넣기

1. 문의접수 스프레드시트를 엽니다
2. 상단 메뉴 **[확장 프로그램] → [Apps Script]**
3. 편집기에 있던 기본 코드(`function myFunction() {}`)를 **전부 지웁니다**
4. `apps-script.gs` 파일 내용을 **전체 복사해서 붙여넣기**
5. 💾 저장 (Ctrl+S / Cmd+S). 프로젝트 이름을 물으면 `퍼널마케팅 문의수집`

### 2-2. 먼저 테스트해 보기

배포 전에 시트에 제대로 들어가는지 확인합니다.

1. 편집기 상단 함수 선택 드롭다운에서 **`테스트_한줄_넣기`** 선택 → **▶ 실행**
2. 처음 실행하면 권한 승인 창이 뜹니다

> **「Google에서 확인하지 않은 앱」 경고가 나옵니다.** 정상입니다.
> **[고급]** → **[퍼널마케팅 문의수집(안전하지 않음)으로 이동]** → **[허용]**
> 본인이 본인 시트에 쓰는 스크립트라 안전합니다.

3. 시트로 돌아가서 **`(주)테스트개발` 줄이 들어왔는지** 확인
4. A열 문의번호와 W열 D-Day가 **자동으로 계산됐는지** 확인
5. 확인했으면 **그 테스트 줄은 지우세요**

여기까지 됐으면 나머지는 거의 끝난 겁니다.

### 2-3. 웹 앱으로 배포

1. 편집기 우측 상단 **[배포] → [새 배포]**
2. 좌측 ⚙️ (톱니바퀴) → **유형 선택: 웹 앱**
3. 설정을 **정확히** 이렇게 맞춥니다:

   | 항목 | 값 |
   |---|---|
   | 설명 | `퍼널마케팅 랜딩페이지` |
   | 다음 사용자로 실행 | **나** |
   | 액세스 권한이 있는 사용자 | **모든 사용자** |

   > **「모든 사용자」가 핵심입니다.** 여기를 「나만」으로 두면 방문자가 신청해도 아무것도 안 들어옵니다.

4. **[배포]** 클릭
5. 나오는 **웹 앱 URL** 복사

   ```
   https://script.google.com/macros/s/AKfycb...긴문자열.../exec
   ```

### 2-4. 사이트에 붙이기

`index.html` 을 열고 `ENDPOINT` 로 검색합니다. (파일 아래쪽 `<script>` 안)

```js
const ENDPOINT = '';   // ← 여기에 붙여넣기
```

↓

```js
const ENDPOINT = 'https://script.google.com/macros/s/AKfycb.../exec';
```

저장하면 끝입니다.

### 2-5. 실제로 되는지 확인

1. 브라우저에서 `index.html` 을 열고 **본인 정보로 직접 신청**해 봅니다
2. 시트에 줄이 들어왔는지 확인
3. 들어왔으면 그 줄 삭제

> **안 들어올 때 체크리스트**
> - 2-3에서 액세스 권한을 「모든 사용자」로 했는지
> - `ENDPOINT` 주소가 `/exec` 로 끝나는지 (`/dev` 아님)
> - 주소 앞뒤 따옴표 `'` 가 살아있는지

### 2-6. 신청 오면 알림 받기

시트에서 **[도구] → [알림 설정]** → *변경사항이 있을 때* + *바로 알림*
신청이 들어올 때마다 메일이 옵니다.

### ⚠️ 코드를 고친 뒤에는

Apps Script를 수정했으면 **[배포] → [배포 관리] → ✏️ 편집 → 버전 「새 버전」 → [배포]**
이걸 안 하면 예전 코드가 계속 돌아갑니다. URL은 그대로 유지됩니다.

---

## 3. 로고 넣기

로고 이미지를 `logo.png` 로 `index.html` 과 같은 폴더에 넣고,
`class="logo"` 로 검색해 나오는 곳(헤더)을 아래처럼 바꿉니다.

```html
<a href="#top" class="logo"><img src="logo.png" alt="퍼널마케팅" style="height:26px"></a>
```

푸터 로고는 `class="ft-logo"` 로 검색하면 나옵니다.

---

## 4. 인터넷에 올리기

| 방법 | 방식 |
|---|---|
| **GitHub Pages** | 저장소에 올리면 자동 배포. 무료, 수정 반영이 명령 한 줄 |
| **Netlify Drop** | [app.netlify.com/drop](https://app.netlify.com/drop) 에 폴더째 드래그. 30초 |
| **가비아 / 카페24** | 도메인이 이미 있으면 FTP로 `index.html` 업로드 |

> 올릴 때 `apps-script.gs` 와 `SETUP.md` 는 **빼고 `index.html` 만** 올리셔도 됩니다.
> 같이 올라가도 사이트 동작에는 지장 없습니다.

---

## 5. 나중에 바꾸고 싶을 때

| 바꿀 것 | `index.html` 에서 검색할 단어 |
|---|---|
| 전화번호 | `01089812510` |
| 이메일 | `dbslzhs1515` |
| 브랜드 색상 | `--blue:` |
| 히어로 문구 | `분양은` |
| 비교표 내용 | `vs-row` |
| 서비스 5단계 | `svc-body` |
| 리포트 예시 수치 | `rtab` |
| 폼 필드 | `leadForm` |
| 시트 연동 주소 | `ENDPOINT` |

폼에 칸을 추가하려면 `index.html` 과 `apps-script.gs` **양쪽 다** 고쳐야 합니다.

---

## 6. 나중에 실적이 쌓이면

02 섹션 위에 **실제 성과 수치 띠**(담당 현장 수 / 평균 유효율 / 누적 DB 수)를
추가하는 게 전환율에 가장 큰 영향을 줍니다. 수치가 생기면 알려주세요.
